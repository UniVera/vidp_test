<?php
if ($this->lecturer) {
    array_push($_SESSION['lecturers'], $this->lecturer);
} elseif ($this->module) {
    array_push($_SESSION['modules'], $this->module);
} elseif ($this->studyProgram) {
    array_push($_SESSION['studyPrograms'], $this->studyProgram);
}
?>