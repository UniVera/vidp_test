<?php
if ($this->lecturer) {
    array_push($_SESSION['lecturers'], $this->lecturer);
} elseif ($this->moduleName) {
    array_push($_SESSION['modules'], $this->moduleName);
} elseif ($this->studyProgramName) {
    array_push($_SESSION['studyPrograms'], $this->studyProgramName);
}
?>